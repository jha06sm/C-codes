#include<stdio.h>
#include<string.h>
int main()
{
    char str[1000];
    int i;
    printf("enter the string:");
    scanf("%s",str);
    printf("length of string is %d",strlen(str));
    return 0;
}
