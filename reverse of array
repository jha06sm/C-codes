#include<stdio.h>
int main()
{
    int a[10],i;
    for(i=0;i<10;i++)
    {
    printf("enter the number:");
    scanf("%d",&a[i]);
    }
    for(i=9;i>=0;i--)
    {
    printf("\nnumbers in reverse order are:%d",a[i]);
    
    }
return 0;
}
